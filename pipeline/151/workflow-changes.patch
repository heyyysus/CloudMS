diff --git a/.github/workflows/agent-docs.yml b/.github/workflows/agent-docs.yml
index 7c4cd3d..c836eac 100644
--- a/.github/workflows/agent-docs.yml
+++ b/.github/workflows/agent-docs.yml
@@ -150,9 +150,7 @@ jobs:
           GH_TOKEN: ${{ secrets.PIPELINE_BOT_TOKEN }}
           TITLE: ${{ github.event.issue.title }}
         run: |
-          # Tee'd for the case report-failure actually falls back to stage.log: only
-          # when `tail` is empty, i.e. claude-run produced no execution file at all.
-          # Whenever it did, the Claude tail wins and this log is unused.
+          # Tee'd so report-failure can append this step's shell output to its comment.
           exec > >(tee -a "$RUNNER_TEMP/stage.log") 2>&1
           set -euo pipefail
           if gh pr list --head "$BRANCH" --state open --json number -q '.[0].number' | grep -q .; then
@@ -206,6 +204,7 @@ jobs:
           stop_reason: ${{ steps.claude.outputs.stop_reason }}
           target: issue
           number: ${{ env.ISSUE }}
+          outcome: stopped early
           resume: >-
             The docs stage stopped early and the PR opened anyway, so this is a notice,
             not a stall. Read the docs part of the diff; to redo them, remove
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index f8c5e6d..ccf6a9b 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -45,6 +45,12 @@ jobs:
       - name: claude-run tail renderer
         run: bash .github/actions/claude-run/render-tail.test.sh

+      - name: report-failure log renderer
+        run: bash .github/actions/report-failure/render-log.test.sh
+
+      - name: report-failure headline renderer
+        run: bash .github/actions/report-failure/render-headline.test.sh
+
   backend:
     needs: changes
     if: needs.changes.outputs.backend == 'true' || github.event_name == 'workflow_dispatch'
