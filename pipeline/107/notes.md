## Docs

no doc changes needed — the implementation stage has not produced any commits yet: the working tree is clean, `.github/workflows/ci.yml`/`backend/vitest.config.ts`/`backend/package.json` show none of the coverage changes described in the approved plan, and the code diff supplied to this stage is empty. Nothing to document.
