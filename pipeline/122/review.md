# Plan review — issue #122

## Findings

- **Scope matches the issue and PROJECT.md.** Backend-only, defers the frontend to sub-issue 9, and lines up with PROJECT.md direction item 2 and `docs/multitenancy.md`'s rollout-order item 9 ("Row-level security backstop (#122)"). The dependency claim (sub-issue 7/9 = #121, merged) checks out against git log (`0ad45ff`) and `docs/multitenancy.md`'s own "Done (#121)" notes.
- **Real conflict correctly found and resolved.** The issue's literal spec (a `SELECT`-only RLS policy on `organizations`) would break `GET /auth/me`: `meResponse` (`backend/src/auth/routes.ts:29-31`) calls `findOrganizationById`/`listActiveMembershipsWithOrg` under `requireSession`, before any org context exists, so the policy would return nothing and the org picker would silently stop working. Verified `requireAuth` (`backend/src/auth/middleware.ts:47-70`) is the only place `req.orgId` is resolved, confirming `/auth/me` never has one. The plan's recommended fix — treat `organizations` like `users`/`sessions`/`org_memberships` (reached only through the unprotected auth layer) — is sound and matches how the issue already carves out those three tables. Correctly flagged as a decision needed before implementation, with the plan noting everything else is unaffected either way.
- **File list and mechanics spot-check clean.** Confirmed against the actual files: `backend/src/db/index.ts`'s current two-pool shape, `roles.ts`'s `set_config`-then-`format('%L', …)` trick (cited rather than re-derived, as instructed), `org_id` is `varchar(22)` via `rowIdFk`/`ids.ts` (no `::int` cast needed), `db:push` currently ends in `roles.ts` (append-only change), CI already runs `npm run db:push` (`ci.yml:75`), all cited `catch` sites in `carriers.ts` exist near the claimed lines, and every jobs/seed/db file the plan says stays on `adminDb` already imports it today. `TestContext.org()`/fixture builders/`cleanup()` match the plan's described current shape closely enough that the described edits are mechanical, not speculative.
- **Tests are appropriately structured.** Cross-tenant suite and RLS proof are new `TestContext`-based tests; the existing-test sweep explicitly reuses `runInOrg` via `testHelpers` rather than inventing a second fixture path. The one global-count assertion in `rls.test.ts` is justified (RLS makes it deterministic regardless of concurrent workers) and the plan proactively notes it's the sole exception to CLAUDE.md's "never assert on a global row count," with a reason.
- **Security direction is right and the design shows real engineering care**, not just relaying the issue: per-request tx pinned to org context, `adminDb` fenced off by both file-layout convention and an ESLint rule, `FORCE ROW LEVEL SECURITY` caveat about superuser owner correctly called out as the one fragile assumption, and the commit-after-response-sent tradeoff is named rather than glossed over.
- **Minor nit, not blocking:** step 6 justifies moving `TestContext.org()`'s insert to `adminDb` by citing the SELECT-only-policy scenario specifically, but if the Risks section's recommended alternative (no RLS on `organizations`) is adopted instead, the same `adminDb` move is still the right call (org creation is inherently owner-side per the not-yet-built onboarding flow) — the stated reasoning just doesn't fully generalize across the still-open decision. Worth a one-line fix during implementation, not a plan defect.

## Required changes (if rejected)
N/A

Verdict: approved
